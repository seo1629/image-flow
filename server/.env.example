# Optional fallback only — the app normally sends the VWorld API key from
# Settings (browser localStorage) with each /api/vworld/search request.
# Set this only if you want address search to work without that.
VWORLD_API_KEY=
